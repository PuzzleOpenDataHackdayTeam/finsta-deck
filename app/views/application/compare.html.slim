- title @result.winner == :me ? 'Gewonnen' : 'Verloren'

= render 'compare'

.progress-bar
  div style="width: #{@result.percent.me}%"
.progress-bar
	div style="width: #{@result.percent.you}%"

= link_to('Weiter gehts', show, data: { role: 'button' })

